#include <iostream>

using namespace std;

int main() {
    setlocale(LC_ALL, "RU");
    double F, C;
    for (F = -100; F <= 100.0; F += 20) {
        C = 5.0 / 9.0 * (F - 32.0);
        cout << "Градусы по F = " << F << " по С = " << C << endl;
    }
       
}
