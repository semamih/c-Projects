#include <iostream>
#include <cmath>
#include <math.h>

using namespace std;




int main() {
    float digit{0};
    cout << "Enter nomber ";
    cin >> digit;

    while (cin.fail() == 1) {
        cin.clear();
        cin.ignore();
        cout << "Enter nomber " << endl;
        cin >> digit;
    }
    while (digit <= 0) {
        cin.clear();
        cin.ignore();
        cout << "Error. Enter nomber " << endl;
        cin >> digit;
    }

    cout << digit << endl;

    system("pause");
    return 0;
}
    
